# `application/cart`

Cart use cases and `CartRepository` interface.

UI (Client Component islands) calls this layer. UI must not use `localStorage` / `sessionStorage` / IndexedDB.

Not implemented until Sprint 4.
