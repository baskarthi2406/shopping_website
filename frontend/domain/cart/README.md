# `domain/cart`

Cart and CartItem rules (add / update quantity / remove, line math).

No browser APIs. Persistence is infrastructure via a repository interface. Cart behavior is Sprint 4, not S1-T04.
