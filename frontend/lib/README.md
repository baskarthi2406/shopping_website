# `lib`

Small shared **technical** utilities only (for example a future `cn` helper).

**Must not contain:** use cases, repository access, domain models, or a miscellaneous dump.

Prefer domain or application when the logic is business-related. Nothing is required in S1-T04.
